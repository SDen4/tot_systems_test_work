import React, {Component} from 'react';

import Login from './login';

class App extends Component {
    state = {
    }
    render() {
        return (
            <Login />
        );
    }
};

export default App;